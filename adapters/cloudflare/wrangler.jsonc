{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "tallywick",
  "main": "src/worker.ts",
  "compatibility_date": "2026-08-01",
  "observability": { "enabled": true },

  "durable_objects": {
    "bindings": [
      { "name": "TALLYWICK", "class_name": "TallywickDO" }
    ]
  },

  "migrations": [
    { "tag": "v1", "new_sqlite_classes": ["TallywickDO"] }
  ],

  // A fork sets ALLOWED_ORIGINS to its own site origin. This deployment serves
  // banjobyster.github.io, which hosts both the portfolio and the bysters demo.
  "vars": {
    "ALLOWED_ORIGINS": "https://banjobyster.github.io",
    "AUTO_CREATE": "true",
    "DEDUP": "none",
    "RATE_LIMIT": "60",
    "REQUIRE_POST": "false"
  }

  // Secrets are set out of band, never in this file:
  //   npx wrangler secret put ADMIN_TOKEN
  //   npx wrangler secret put IP_SALT      (optional)
}
